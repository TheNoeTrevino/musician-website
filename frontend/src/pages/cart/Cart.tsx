import CartSection from "../../components/CartSection";

const Cart = () => {
  return (
    <body className="flex flex-col  bg-black">
      <CartSection />
    </body>
  );
};

export default Cart;
